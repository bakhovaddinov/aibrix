 => ERROR [2/3] RUN apt-get update && apt-get install -y tmux                                                                                                                                               7.2s
------
 > [2/3] RUN apt-get update && apt-get install -y tmux:
0.057 Incorrect Usage: flag provided but not defined: -keep
0.057
0.057 NAME:
0.057    runc run - create and run a container
0.057
0.057 USAGE:
0.057    runc run [command options] <container-id>
0.057
0.057 Where "<container-id>" is your name for the instance of the container that you
0.057 are starting. The name you provide for the container instance must be unique on
0.057 your host.
0.057
0.057 DESCRIPTION:
0.057    The run command creates an instance of a container for a bundle. The bundle
0.057 is a directory with a specification file named "config.json" and a root
0.057 filesystem.
0.057
0.057 The specification file includes an args parameter. The args parameter is used
0.057 to specify command(s) that get run when the container is started. To change the
0.057 command(s) that get executed on start, edit the args parameter of the spec. See
0.057 "runc spec --help" for more explanation.
0.057
0.057 OPTIONS:
0.057    --bundle value, -b value  path to the root of the bundle directory, defaults to the current directory
0.057    --console-socket value    path to an AF_UNIX socket which will receive a file descriptor referencing the master end of the console's pseudoterminal
0.057    --detach, -d              detach from the container's process
0.057    --pid-file value          specify the file to write the process id to
0.057    --no-subreaper            disable the use of the subreaper used to reap reparented processes
0.057    --no-pivot                do not use pivot root to jail process inside rootfs.  This should be used whenever the rootfs is on top of a ramdisk
0.057    --no-new-keyring          do not create a new session keyring for the container.  This will cause the container to inherit the calling processes session key
0.057    --preserve-fds value      Pass N additional file descriptors to the container (stdio + $LISTEN_FDS + N in total) (default: 0)
0.057
0.060 flag provided but not defined: -keep
------
Dockerfile:3
--------------------
   1 |     FROM quay.io/ascend/vllm-ascend:v0.18.0
   2 |
   3 | >>> RUN apt-get update && apt-get install -y tmux
   4 |     RUN pip install openyuanrong-transfer-engine
   5 |
--------------------
error: failed to solve: process "/bin/bash -c apt-get update && apt-get install -y tmux" did not complete successfully: exit code: 1
FATA[0170] no image was built
